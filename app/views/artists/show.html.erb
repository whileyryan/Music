
  <%= render 'artist' %>


  <% if user_signed_in? && @reviews.length > 0%>
  <div class="reviews-container">
      <% @reviews.each do |review| %>
        <%= render partial:'/reviews/review', locals:{review:review} %>
      <% end %>
    <% end %>

  </div>
</div>
